# Server Environment Configuration
PORT=5002

# MongoDB Connection String (Replace with your local or Atlas cluster URI)
MONGO_URI=mongodb+srv://bvinothinicct_db_user:<_09Cct>@cluster0.al0h3ik.mongodb.net/?appName=Cluster0

# Secure JWT Secret (Generate a strong random 64-char hex string: e.g. openssl rand -hex 64)
JWT_SECRET=replace_with_a_secure_random_hex_string

# Allowed Frontend URL for CORS (e.g. http://localhost:5173,https://app.yourdomain.com)
FRONTEND_URL=http://localhost:5173
